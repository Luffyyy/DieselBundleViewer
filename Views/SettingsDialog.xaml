<UserControl x:Class="DieselBundleViewer.Views.SettingsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="400" Height="200">
    <DockPanel Margin="8" LastChildFill="False">
        <StackPanel DockPanel.Dock="Top">
            <CheckBox Content="Extract the full directory of files when saving" IsChecked="{Binding ExtractFullDir, Mode=TwoWay}"/>
            <CheckBox Content="Display 0 byte files" IsChecked="{Binding DisplayEmptyFiles, Mode=TwoWay}">
                <CheckBox.ToolTip>
                    There are certain files that show up inside the game but they don't really exist in the bundles. The option toggles their visiblity.
                </CheckBox.ToolTip>
            </CheckBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" DockPanel.Dock="Bottom">
            <Button Content="Save" Padding="8" Command="{Binding CloseDialog}" CommandParameter="True"/>
            <Button Margin="2 0" Padding="8" Content="Cancel" Command="{Binding CloseDialog}" CommandParameter="False"/>
        </StackPanel>
    </DockPanel>
</UserControl>
